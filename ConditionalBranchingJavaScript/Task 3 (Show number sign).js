if (OS === 'Linux') {
    console.log ("You've got the Linux!");
} else if (OS === 'MacOS'
    || OS === 'Windows XP'
    || OS === 'Windows 7'
    || OS === 'Windows 8') {
    console.log ( 'Okay we support these OS too' );
} else {
    console.log ( 'We hope that this OS is ok!' );
}